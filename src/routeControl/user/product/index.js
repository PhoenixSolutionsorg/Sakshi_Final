import { baseRoutes } from "../../../helpers/baseRoutes";

const accessRoute = {
    PRODUCTS : {
        path : `${baseRoutes.userBaseRoutes}product`
    },
    DESCRIPTION : {
        path : `${baseRoutes.userBaseRoutes}description`
    }
}

export default accessRoute;